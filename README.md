# Flex Tennis — Coming Soon

Static landing page announcing Flex Tennis, an iOS-first platform for automated flex tennis tournaments, launching in Chicago.

Plain HTML/CSS/JS, served directly via GitHub Pages (no build step, no Jekyll).

- `index.html` — page markup and copy
- `styles.css` — styling
- `script.js` — email signup handling (stored locally; no backend yet)
